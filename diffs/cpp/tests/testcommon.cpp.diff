--- cpp/tests/testcommon.cpp [lightvector:master]+++ cpp/tests/testcommon.cpp [hzyhhzy:Reversi2022]@@ -6,10 +6,6 @@   for(int i = 0; i<Board::MAX_ARR_SIZE; i++)
     if(b1.colors[i] != b2.colors[i])
       return false;
-  if(b1.numBlackCaptures != b2.numBlackCaptures)
-    return false;
-  if(b1.numWhiteCaptures != b2.numWhiteCaptures)
-    return false;
   return true;
 }
 
